query bookings($occupantId: Float) {
  bookings(occupantId: $occupantId) {
    id
    offer {
      id
      title
      description
      priceHT
      priceTTC
    }
    startDate
    endDate
  }
}
